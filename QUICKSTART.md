# ⚡ Quick Start Guide - เริ่มต้นใช้งาน 5 นาที

## 🎯 ทดสอบระบบในเครื่องของคุณ

### ขั้นตอนที่ 1: ติดตั้ง Dependencies (1 นาที)

```bash
# เข้าโฟลเดอร์โปรเจค
cd wifi-register

# ติดตั้ง packages
npm install
```

### ขั้นตอนที่ 2: รันโปรเจค (30 วินาที)

```bash
npm run dev
```

### ขั้นตอนที่ 3: เปิดเบราว์เซอร์

```
http://localhost:3000
```

### ขั้นตอนที่ 4: ทดสอบลงทะเบียน (2 นาที)

1. กรอกข้อมูล:
   - ชื่อ: `ทดสอบ ระบบ`
   - เบอร์: `0812345678`
   - อีเมล: (ไม่บังคับ)
   - ✅ ยอมรับเงื่อนไข

2. กดปุ่ม "เชื่อมต่อ WiFi"

3. ✅ ควรเห็นหน้าแสดงรหัส WiFi

---

## 🎨 สร้าง QR Code (2 นาที)

### ขั้นตอนที่ 1: ติดตั้ง package

```bash
npm install
```

### ขั้นตอนที่ 2: แก้ไข URL (ถ้าต้องการ)

เปิดไฟล์: `scripts/generate-qr.js`

```javascript
// บรรทัดที่ 4: เปลี่ยน URL นี้
const REGISTRATION_URL = 'http://localhost:3000';
// หรือ 'https://wifi.yourhospital.com'
```

### ขั้นตอนที่ 3: สร้าง QR Code

```bash
npm run generate-qr
```

### ขั้นตอนที่ 4: ดู QR Code

```
เปิดไฟล์: public/qr-codes/preview.html
```

---

## 🚀 Deploy ไปยัง Vercel (5 นาที)

### ขั้นตอนที่ 1: สร้างบัญชี Vercel

1. เข้า https://vercel.com
2. Sign up ด้วย GitHub (ฟรี)

### ขั้นตอนที่ 2: ติดตั้ง Vercel CLI

```bash
npm install -g vercel
```

### ขั้นตอนที่ 3: Login

```bash
vercel login
```

### ขั้นตอนที่ 4: Deploy

```bash
cd wifi-register
vercel
```

ตอบคำถาม:
```
? Set up and deploy "wifi-register"? [Y/n] Y
? Which scope? [Your account]
? Link to existing project? [y/N] N
? What's your project's name? wifi-register
? In which directory is your code located? ./
```

### ขั้นตอนที่ 5: เสร็จสิ้น!

```
✅ Production: https://wifi-register-xxx.vercel.app
```

---

## 📱 ทดสอบ QR Code บนมือถือ (3 นาที)

### วิธีที่ 1: ใช้ ngrok (แชร์ localhost ออกอินเทอร์เน็ต)

```bash
# ติดตั้ง ngrok
npm install -g ngrok

# รัน Next.js
npm run dev

# เปิด terminal ใหม่, รัน ngrok
ngrok http 3000
```

ได้ URL: `https://xxxx.ngrok.io`

### วิธีที่ 2: Deploy Vercel แล้วสร้าง QR ใหม่

```bash
# แก้ไข scripts/generate-qr.js
# เปลี่ยน URL เป็น Vercel URL
const REGISTRATION_URL = 'https://wifi-register-xxx.vercel.app';

# สร้าง QR ใหม่
npm run generate-qr

# ลองสแกน!
```

---

## ✅ Checklist การทดสอบ

### Local Testing:
- [ ] รันโปรเจค `npm run dev` ได้
- [ ] เปิด http://localhost:3000 เห็นหน้าลงทะเบียน
- [ ] กรอกฟอร์มและ submit ได้
- [ ] แสดงรหัส WiFi ออกมา
- [ ] ปุ่มคัดลอกรหัสใช้งานได้

### QR Code:
- [ ] สร้าง QR Code ได้ `npm run generate-qr`
- [ ] ไฟล์ QR Code อยู่ใน `public/qr-codes/`
- [ ] เปิด preview.html เห็น QR Code ทั้งหมด
- [ ] สแกน QR Code เข้าหน้าลงทะเบียนได้

### Deployment:
- [ ] Deploy Vercel สำเร็จ
- [ ] เปิด Production URL ได้
- [ ] ทดสอบลงทะเบียนบน Production
- [ ] สแกน QR Code บนมือถือได้

---

## 🎯 ขั้นตอนถัดไป

หลังจากทดสอบเสร็จแล้ว:

### 1. ปรับแต่งระบบ
- [ ] เปลี่ยน Title ในหน้าเว็บ
- [ ] เปลี่ยนสี Theme (ถ้าต้องการ)
- [ ] ใส่โลโก้โรงพยาบาล

### 2. ตั้งค่า Router
- [ ] อ่าน [DEPLOYMENT.md](./DEPLOYMENT.md)
- [ ] เตรียม Router ที่รองรับ Captive Portal
- [ ] ตั้งค่า Guest WiFi
- [ ] ทดสอบ Captive Portal

### 3. เตรียม QR Code
- [ ] อ่าน [QR-CODE-SETUP.md](./QR-CODE-SETUP.md)
- [ ] สร้าง QR Code ด้วย Production URL
- [ ] ออกแบบป้าย/โปสเตอร์
- [ ] พิมพ์และติดตั้ง

### 4. ทดสอบกับผู้ใช้จริง
- [ ] ให้เจ้าหน้าที่ทดสอบ
- [ ] ปรับปรุงตาม Feedback
- [ ] เปิดใช้งาน Production

---

## 🆘 แก้ปัญหาเบื้องต้น

### ปัญหา: npm install ไม่ได้

**แก้ไข:**
```bash
# ลบ node_modules และติดตั้งใหม่
rm -rf node_modules package-lock.json
npm install
```

### ปัญหา: npm run dev ไม่ทำงาน

**แก้ไข:**
```bash
# ตรวจสอบ port 3000 ว่าถูกใช้งานหรือไม่
# Windows:
netstat -ano | findstr :3000

# Mac/Linux:
lsof -i :3000

# เปลี่ยน port
npm run dev -- -p 3001
```

### ปัญหา: QR Code ไม่ถูกสร้าง

**แก้ไข:**
```bash
# ตรวจสอบว่าติดตั้ง qrcode แล้ว
npm list qrcode

# ถ้ายังไม่มี ติดตั้งใหม่
npm install qrcode

# รันอีกครั้ง
npm run generate-qr
```

### ปัญหา: Deploy Vercel ไม่ได้

**แก้ไข:**
```bash
# Login ใหม่
vercel logout
vercel login

# Deploy อีกครั้ง
vercel --prod
```

---

## 💡 Tips เพิ่มเติม

### ทดสอบบนมือถือในเครือข่ายเดียวกัน

```bash
# รัน Next.js
npm run dev

# หา IP ของคอมพิวเตอร์
# Windows:
ipconfig

# Mac/Linux:
ifconfig

# เปิดบนมือถือ (ต้องต่อ WiFi เดียวกัน)
http://192.168.1.XXX:3000
```

### ใช้ Mock Data สำหรับทดสอบ

กรอกข้อมูลทดสอบ:
```
ชื่อ: Test User
เบอร์: 0812345678
อีเมล: test@example.com
```

### สำรอง QR Code ไว้หลายแห่ง

```bash
# Copy QR Code ไปยัง Cloud
# Google Drive, Dropbox, etc.
```

---

## ⏱ Timeline การ Setup ทั้งหมด

```
Day 1 (1 ชั่วโมง):
  ✅ ติดตั้งและทดสอบ Local
  ✅ Deploy Vercel
  ✅ สร้าง QR Code

Day 2 (3-4 ชั่วโมง):
  ✅ ซื้อและติดตั้ง Router
  ✅ ตั้งค่า Captive Portal
  ✅ ทดสอบระบบ

Day 3 (2 ชั่วโมง):
  ✅ พิมพ์ QR Code
  ✅ ติดตามจุดต่างๆ
  ✅ Train เจ้าหน้าที่

Day 4:
  ✅ เปิดใช้งาน Production!
```

---

## 🎉 พร้อมแล้ว!

หลังจากทำตาม Quick Start นี้แล้ว คุณควรจะมี:

- ✅ ระบบลงทะเบียน WiFi ที่ทำงานได้
- ✅ QR Code พร้อมใช้งาน
- ✅ Production URL บน Vercel

**ขั้นตอนถัดไป:**
- อ่าน [DEPLOYMENT.md](./DEPLOYMENT.md) สำหรับติดตั้งจริง
- อ่าน [FAQ.md](./FAQ.md) ตอบคำถามเรื่อง Captive Portal

---

**Happy Coding! 🚀**
