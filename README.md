# 🏥 WiFi Registration System - ระบบลงทะเบียน WiFi สำหรับโรงพยาบาล

ระบบลงทะเบียน WiFi แบบ Captive Portal ที่ช่วยให้ผู้ป่วยและผู้มาเยี่ยมสามารถเข้าใช้งาน WiFi ได้อย่างง่ายดาย แม้ไม่มีอินเทอร์เน็ต

![WiFi Registration Demo](https://via.placeholder.com/800x400?text=WiFi+Registration+System)

## ✨ Features

- 📱 **ใช้งานง่าย** - สแกน QR Code หรือเชื่อมต่อ Guest WiFi
- 🔒 **ปลอดภัย** - แยก Network และจำกัดสิทธิ์การใช้งาน
- ⏱ **จำกัดเวลา** - ใช้งานได้ 2 ชั่วโมงต่อครั้ง
- 📊 **Responsive** - ใช้งานได้ทั้งมือถือและคอมพิวเตอร์
- 🎨 **UI สวยงาม** - ออกแบบเป็นมิตรกับผู้ใช้
- 🔐 **รหัสอัตโนมัติ** - สร้างรหัส WiFi อัตโนมัติ ไม่ซ้ำ

## 🎯 ทำไมต้องใช้ระบบนี้?

### ปัญหาที่แก้ไขได้:

❌ **ปัญหาเดิม:**
- ลูกค้าต้องถามรหัส WiFi จากเจ้าหน้าที่
- รหัส WiFi เดียวกันทุกคน ไม่ปลอดภัย
- ไม่สามารถจำกัดเวลาใช้งาน
- ไม่มีข้อมูลผู้ใช้งาน

✅ **หลังใช้ระบบนี้:**
- ลูกค้าลงทะเบียนเองได้ภายใน 1 นาที
- รหัสไม่ซ้ำ สามารถติดตามได้
- จำกัดเวลาใช้งานอัตโนมัติ
- มีข้อมูลสำหรับ Analytics

## 🚀 Quick Start

### 1. ติดตั้ง Dependencies

```bash
cd wifi-register
npm install
```

### 2. รันโปรเจค (Development)

```bash
npm run dev
```

เปิดเบราว์เซอร์: http://localhost:3000

### 3. Deploy Production

```bash
# Deploy ไปยัง Vercel
npm install -g vercel
vercel
```

## 📋 วิธีใช้งาน (สำหรับลูกค้า)

### กรณีที่ 1: มี WiFi Guest + QR Code

```
1. เชื่อมต่อ WiFi "Hospital-Guest" (ไม่ต้องใส่รหัส)
2. สแกน QR Code 
3. กรอกข้อมูล: ชื่อ, เบอร์โทร
4. ได้รหัส WiFi ทันที!
5. เชื่อมต่อ WiFi หลัก "Hospital-WiFi" ด้วยรหัสที่ได้
```

### กรณีที่ 2: Captive Portal (แนะนำ)

```
1. เชื่อมต่อ WiFi "Hospital-Guest"
2. เปิดเบราว์เซอร์ → เข้าหน้าลงทะเบียนอัตโนมัติ
3. กรอกข้อมูล
4. ใช้งานอินเทอร์เน็ตได้เลย!
```

## 🛠 Technology Stack

- **Frontend:** Next.js 15, React, TypeScript
- **Styling:** Tailwind CSS
- **Backend:** Next.js API Routes
- **Database:** Vercel Postgres / Supabase (Optional)
- **Hosting:** Vercel
- **Network:** UniFi / MikroTik Router

## 📁 โครงสร้างโปรเจค

```
wifi-register/
├── app/
│   ├── page.tsx              # หน้าลงทะเบียน
│   ├── layout.tsx            # Layout หลัก
│   ├── globals.css           # Global styles
│   └── api/
│       └── register/
│           └── route.ts      # API สำหรับลงทะเบียน
├── public/
│   └── qr-codes/            # QR Code files
├── DEPLOYMENT.md            # คู่มือการติดตั้ง
├── QR-CODE-SETUP.md        # วิธีสร้าง QR Code
└── README.md
```

## 🔧 Configuration

### Environment Variables

สร้างไฟล์ `.env.local`:

```env
# Database (Optional)
DATABASE_URL=postgresql://...

# Router API (Optional - สำหรับ auto-provisioning)
ROUTER_TYPE=unifi
UNIFI_HOST=https://unifi-controller:8443
UNIFI_USERNAME=admin
UNIFI_PASSWORD=password
UNIFI_SITE=default

# WiFi Settings
WIFI_SSID=Hospital-WiFi
WIFI_SESSION_DURATION=7200  # 2 hours in seconds
```

## 📱 QR Code Setup

อ่านคู่มือฉบับเต็ม: [QR-CODE-SETUP.md](./QR-CODE-SETUP.md)

### สร้าง QR Code ง่ายๆ:

```bash
# ติดตั้ง package
npm install qrcode

# สร้าง QR Code
node scripts/generate-qr.js
```

QR Code จะถูกสร้างใน `/public/qr-codes/`

## 🏥 การติดตั้งในโรงพยาบาล

อ่านคู่มือฉบับเต็ม: [DEPLOYMENT.md](./DEPLOYMENT.md)

### ขั้นตอนสรุป:

1. ✅ Deploy แอพพลิเคชันไปยัง Vercel
2. ✅ ตั้งค่า Router (UniFi/MikroTik) สำหรับ Captive Portal
3. ✅ สร้างและพิมพ์ QR Code
4. ✅ ติด QR Code ตามจุดต่างๆ
5. ✅ ทดสอบระบบ
6. ✅ Train เจ้าหน้าที่
7. ✅ เปิดใช้งาน

## 🔒 Security Features

- ✅ แยก Guest Network จาก Staff Network
- ✅ จำกัด Bandwidth (5 Mbps/user)
- ✅ จำกัดเวลาใช้งาน (2 ชั่วโมง)
- ✅ Block P2P และ Torrent
- ✅ Content Filtering
- ✅ เข้ารหัสข้อมูลส่วนตัว (HTTPS)
- ✅ ลบข้อมูลเก่าอัตโนมัติ (90 วัน)

## 📊 Analytics & Monitoring

### ข้อมูลที่ติดตาม:

- จำนวนผู้ลงทะเบียนต่อวัน
- อุปกรณ์ที่ใช้งาน (iOS/Android)
- เวลาที่มีการใช้งานมาก
- Error Rate

### เพิ่ม Analytics:

```bash
npm install @vercel/analytics
```

```tsx
// app/layout.tsx
import { Analytics } from '@vercel/analytics/react';

export default function Layout({ children }) {
  return (
    <html>
      <body>
        {children}
        <Analytics />
      </body>
    </html>
  );
}
```

## 🧪 Testing

### ทดสอบ Form:

```bash
npm run dev
```

เปิด http://localhost:3000 และทดสอบ:
- [ ] กรอกข้อมูลครบ → ได้รหัส WiFi
- [ ] กรอกเบอร์ไม่ครบ 10 หลัก → Error
- [ ] ไม่ยอมรับเงื่อนไข → ไม่สามารถกด Submit
- [ ] คัดลอกรหัส WiFi → Clipboard

## 🎨 Customization

### เปลี่ยนสี Theme:

แก้ไขใน `app/globals.css`:

```css
:root {
  --color-primary: #3b82f6;    /* Blue */
  --color-secondary: #6366f1;   /* Indigo */
}
```

### เปลี่ยนโลโก้:

แทนที่ไฟล์ `public/hospital-logo.png`

### เปลี่ยนเวลาใช้งาน:

แก้ไขใน `app/api/register/route.ts`:

```typescript
// เปลี่ยนจาก 2 ชม. เป็น 4 ชม.
expires_at: new Date(Date.now() + 4 * 60 * 60 * 1000)
```

## 🆘 Troubleshooting

### ปัญหา: สแกน QR แล้วไม่เข้าหน้าลงทะเบียน

**แก้ไข:**
1. ตรวจสอบเชื่อมต่อ WiFi "Hospital-Guest" แล้ว
2. ตรวจสอบ Firewall ว่าเปิดให้เข้า Domain ได้
3. ลองเปิดเบราว์เซอร์แล้วพิมพ์ URL เอง

### ปัญหา: ลงทะเบียนแล้วแต่ไม่มีเน็ต

**แก้ไข:**
1. ตรวจสอบ Router API integration
2. ตรวจสอบ Firewall Rules
3. Restart WiFi บนอุปกรณ์

### ปัญหา: Error 500 ตอน Submit Form

**แก้ไข:**
1. ดู Logs ใน Vercel Dashboard
2. ตรวจสอบ Database connection
3. ตรวจสอบ Environment Variables

## 📚 เอกสารเพิ่มเติม

- [DEPLOYMENT.md](./DEPLOYMENT.md) - คู่มือการติดตั้งแบบละเอียด
- [QR-CODE-SETUP.md](./QR-CODE-SETUP.md) - วิธีสร้างและติดตั้ง QR Code
- [Next.js Documentation](https://nextjs.org/docs)
- [Tailwind CSS](https://tailwindcss.com)

## 🤝 Contributing

ต้องการพัฒนาต่อ? Welcome!

```bash
# Fork repo
# Clone
git clone https://github.com/yourusername/wifi-register.git

# สร้าง branch ใหม่
git checkout -b feature/new-feature

# Commit
git commit -m "Add new feature"

# Push
git push origin feature/new-feature

# Create Pull Request
```

## 📄 License

MIT License - ใช้งานได้ฟรีทั้งโรงพยาบาลเอกชนและรัฐ

## 📞 Support

หากต้องการความช่วยเหลือ:
- 📧 Email: support@yourhospital.com
- 💬 Line: @hospital-it
- 📱 Tel: 02-XXX-XXXX

## 🎉 ขอบคุณ

สร้างด้วย ❤️ สำหรับโรงพยาบาลและผู้ป่วย

---

**Made with Next.js + TypeScript + Tailwind CSS**
